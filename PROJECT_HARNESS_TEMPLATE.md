# PROJECT HARNESS TEMPLATE

이 문서는 개별 프로젝트 폴더 안에서 사용하는 하네스 제어 문서의 템플릿이다. 새 프로젝트를 만들 때 이 파일을 프로젝트 폴더로 복사한 뒤 `HARNESS.md` 또는 `PROJECT_HARNESS.md`로 이름을 바꿔 사용한다.

프로젝트의 모든 에이전트 작업은 이 문서를 기준으로 목표, 입력/출력, 검증 방법, 완료 기준을 확인한다.

## 1. 프로젝트 개요

### 프로젝트 이름

```text
TODO: 프로젝트 이름
```

### 한 줄 목표

```text
TODO: 이 프로젝트가 해결하려는 문제를 한 문장으로 작성
```

### 사용자 시나리오

```text
TODO: 사용자가 어떤 입력을 넣고 어떤 결과를 기대하는지 작성
```

### 현재 단계

```text
TODO: 아이디어 / 프로토타입 / MVP / 실험 / 리팩터링 / 배포 준비 중 하나로 작성
```

## 2. 사용 모델

### Hugging Face 모델 ID

```text
TODO: 예) openai/clip-vit-base-patch32
```

### 태스크 유형

```text
TODO: 예) text-generation, image-classification, zero-shot-classification, speech-to-text
```

### 모델 라이선스

```text
TODO: Hugging Face 모델 카드 기준 라이선스 작성
```

### 실행 요구사항

```text
Python:
CPU/GPU:
VRAM:
주요 패키지:
대략적인 모델 크기:
```

### 모델 다운로드 정책

- 대형 모델 파일 다운로드 전에는 용량, 저장 위치, 목적을 사용자에게 보고하고 승인받는다.
- 모델 캐시, 가중치, 데이터셋은 기본적으로 Git에 포함하지 않는다.
- API 토큰이나 인증 정보는 코드와 문서에 직접 쓰지 않는다.

## 3. 입력/출력 계약

### 입력

```text
TODO: 입력 형식, 예시, 제한사항 작성
```

예:

```json
{
  "text": "분석할 문장"
}
```

### 출력

```text
TODO: 출력 형식, 예시, 성공/실패 응답 작성
```

예:

```json
{
  "label": "positive",
  "score": 0.98
}
```

### 실패 응답

```text
TODO: 잘못된 입력, 모델 로딩 실패, 추론 실패 시 기대 동작 작성
```

## 4. 최소 실행 하네스

이 프로젝트의 첫 번째 목표는 전체 앱이 아니라 가장 작은 실행 단위를 통과시키는 것이다.

### 최소 성공 기준

```text
TODO: 예) 샘플 입력 1개를 넣었을 때 모델이 정상적으로 결과를 반환한다.
```

### 최소 실행 명령

```powershell
TODO: 예) python scripts/smoke_test.py
```

### 기대 결과

```text
TODO: 명령 실행 후 터미널에 보여야 할 핵심 출력 작성
```

## 5. 검증 명령

프로젝트의 변경 사항은 가능한 한 아래 명령으로 검증한다.

### 스모크 테스트

```powershell
TODO: python scripts/smoke_test.py
```

### 단위 테스트

```powershell
TODO: pytest
```

### 타입/정적 검사

```powershell
TODO: mypy src
```

### 포맷 검사

```powershell
TODO: ruff check .
```

사용하지 않는 검증 항목은 삭제하지 말고 `현재 미사용`이라고 표시한다.

## 6. 실패 재현 절차

버그가 발견되면 다음 순서를 따른다.

1. 버그를 재현하는 입력을 기록한다.
2. 실패하는 명령 또는 테스트를 만든다.
3. 실패 로그의 핵심 줄을 기록한다.
4. 최소 코드만 수정한다.
5. 같은 명령이 통과하는지 확인한다.

### 재현 입력

```text
TODO: 버그를 재현하는 입력 또는 파일 경로
```

### 실패 명령

```powershell
TODO: 실패를 재현하는 명령
```

### 기대 동작

```text
TODO: 원래 기대했던 동작
```

### 실제 동작

```text
TODO: 실제로 발생한 오류 또는 잘못된 결과
```

## 7. 변경 승인 규칙

이 프로젝트 안에서 다음 작업은 사용자 승인 후 진행한다.

- 파일 생성, 수정, 삭제, 이동
- 폴더 구조 변경
- 의존성 추가, 제거, 버전 변경
- 모델 다운로드 또는 캐시 위치 변경
- 데이터셋 다운로드
- 환경 변수, 토큰, 인증 설정 변경
- Git 커밋, 푸쉬, 브랜치 변경
- 테스트 스냅샷 갱신
- 대규모 리팩터링 또는 포맷팅

승인 요청에는 다음을 포함한다.

1. 변경할 파일과 폴더
2. 변경 이유
3. 예상 영향 범위
4. 검증 방법

## 8. 작업 원칙

- 먼저 목표와 성공 기준을 적는다.
- 전체 앱보다 작은 하네스를 먼저 만든다.
- 버그는 재현 후 수정한다.
- 요청과 직접 관련 없는 코드는 바꾸지 않는다.
- 새 추상화는 중복이나 복잡도를 실제로 줄일 때만 만든다.
- 임시 코드는 표시하고, 완료 전에 제거하거나 남기는 이유를 기록한다.
- 검증하지 못한 부분은 완료 보고에 반드시 적는다.

## 9. 공개 저장소 체크리스트

이 프로젝트가 GitHub 등 공개 저장소에 올라갈 가능성이 있다면, 공개 전 다음을 확인한다.

- 개인 사용자명이 들어간 로컬 경로를 제거하거나 일반화했다.
- `C:\Users\...` 같은 절대 경로를 `<PROJECT_ROOT>`, `<LOCAL_VENV_PATH>`, `<HF_CACHE_DIR>` 같은 플레이스홀더로 바꿨다.
- `.env`, API 키, 토큰, 개인 키, 인증 정보가 포함되지 않았다.
- 모델 캐시, 가중치, 대형 데이터셋, 임시 출력물이 포함되지 않았다.
- README, HARNESS, 예시 코드가 특정 개인 PC에만 맞춰져 있지 않다.
- 공개하면 안 되는 비공개 프로젝트명, 내부 URL, 계정명이 남아 있지 않다.

문서에 경로 예시가 필요하면 다음처럼 작성한다.

```text
<PROJECT_ROOT>
<LOCAL_VENV_PATH>
<HF_CACHE_DIR>
<DATA_DIR>
```

## 10. 금지 사항

- 사용자 승인 없는 파일/폴더 변경
- 사용자 승인 없는 의존성 설치
- 사용자 승인 없는 대형 모델 또는 데이터 다운로드
- `.env`, 토큰, 개인 키, 인증 정보 커밋
- 검증 없이 "완료"라고 보고
- 관련 없는 코드 정리, 포맷팅, 리팩터링
- 실패 로그나 테스트 실패를 숨기기

## 11. 완료 기준

이 프로젝트의 작업은 다음 조건을 만족해야 완료로 본다.

- 최소 실행 하네스가 통과한다.
- 프로젝트에 정의된 검증 명령이 통과하거나, 실행하지 못한 이유가 기록된다.
- 입력/출력 계약이 문서와 실제 동작에서 일치한다.
- 변경된 파일이 보고된다.
- 남은 위험이나 미확인 사항이 보고된다.

## 12. 작업 로그

작업 로그는 최신 항목을 위에 추가한다.

```text
YYYY-MM-DD
- 작업자:
- 요청:
- 변경 파일:
- 실행한 검증:
- 결과:
- 남은 이슈:
```

## 13. 프로젝트별 메모

```text
TODO: 이 프로젝트에서만 필요한 규칙, 모델 특성, 데이터 경로, 성능 기준 등을 작성
```
