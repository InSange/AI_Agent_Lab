# AGENTS.md

이 저장소는 Hugging Face에 공개된 AI 모델을 활용해 다양한 프로그램, 애플리케이션, 도구를 만드는 작업 폴더다. Codex, Claude, Cursor, 기타 AI 에이전트는 이 문서를 먼저 읽고 아래 규칙을 따른다.

## 기본 원칙

- 모든 대화, 보고, 문서 작성은 기본적으로 한국어로 한다.
- 사용자가 명시적으로 요청하지 않은 기능, 구조, 추상화는 추가하지 않는다.
- 작업은 작게 나누고, 각 단계마다 확인 가능한 성공 기준을 둔다.
- 코드와 폴더 구조에 직접 영향을 주는 작업은 반드시 사전에 보고하고 승인받은 뒤 진행한다.

## 승인 규칙

다음 작업은 실행 전에 사용자에게 먼저 보고하고 승인을 받아야 한다.

- 파일 생성, 수정, 삭제, 이동
- 폴더 구조 생성, 변경, 삭제
- 의존성 설치 또는 제거
- 가상환경 생성, 이동, 재구성
- 대형 모델 파일 다운로드
- Git 커밋, 태그, 브랜치 변경, 푸쉬, 리베이스, 머지
- 기존 코드의 리팩터링 또는 포맷팅
- 외부 서비스 토큰, 인증 정보, 환경 변수 설정 변경

보고할 때는 다음을 포함한다.

1. 무엇을 바꿀지
2. 왜 필요한지
3. 어떤 파일이나 폴더에 영향이 있는지
4. 어떻게 검증할지

승인을 받기 전에는 읽기, 분석, 검색, 계획 수립까지만 수행한다.

## Karpathy식 하네스 엔지니어링

모든 프로젝트는 목표 기반 실행과 검증 가능한 하네스를 중심으로 진행한다.

### 1. 구현 전에 목표를 정의한다

작업을 시작하기 전에 다음을 명확히 한다.

- 해결하려는 문제
- 기대 입력과 출력
- 최소 성공 기준
- 실패했을 때 관찰할 수 있는 증상
- 검증 명령, 테스트, 또는 샘플 실행 방법

모호한 요청은 조용히 추측하지 않는다. 가능한 해석을 제시하고 필요한 질문을 한다.

### 2. 작은 하네스부터 만든다

모델, 파이프라인, UI, API를 바로 크게 만들지 않는다. 먼저 가장 작은 실행 단위를 만든다.

예:

- 모델 로딩만 검증하는 스크립트
- 샘플 입력 1개에 대한 추론 스크립트
- 기대 출력 형태를 확인하는 테스트
- 실패 케이스를 재현하는 최소 코드
- API라면 단일 엔드포인트의 요청/응답 검증
- UI라면 핵심 사용자 흐름 1개 검증

### 3. 버그는 먼저 재현한다

버그 수정은 가능한 한 다음 순서로 진행한다.

1. 버그를 재현하는 테스트나 최소 실행 하네스를 만든다.
2. 그 하네스가 실패하는 것을 확인한다.
3. 필요한 최소 코드만 수정한다.
4. 같은 하네스가 통과하는지 확인한다.
5. 관련 기존 테스트가 있다면 함께 실행한다.

### 4. 성공 기준까지 반복한다

"작동하게 만들기" 같은 모호한 목표로 끝내지 않는다. 다음처럼 확인 가능한 형태로 바꾼다.

- `python scripts/smoke_test.py`가 성공한다.
- 샘플 이미지 3개가 모두 분류된다.
- API가 입력 A에 대해 응답 B를 반환한다.
- UI에서 파일 업로드 후 결과가 화면에 표시된다.
- 모델 로딩, 추론, 후처리 시간이 기록된다.

## 4대 행동 원칙

이 저장소의 에이전트는 Andrej Karpathy의 LLM 코딩 관찰에서 나온 다음 원칙을 따른다.

### 1. 생각 후 코딩

- 가정을 명시한다.
- 불확실하면 질문한다.
- 여러 해석이 가능하면 조용히 하나를 고르지 않는다.
- 더 단순한 접근이 있으면 사용자에게 말한다.
- 혼란스러운 지점이 있으면 멈추고 무엇이 불명확한지 설명한다.

### 2. 단순성 우선

- 오늘 필요한 문제만 푼다.
- 단일 용도 코드에 추상화를 만들지 않는다.
- 요청받지 않은 확장성, 설정화, 플러그인 구조를 만들지 않는다.
- 불가능하거나 아직 관찰되지 않은 오류를 위해 과도한 처리를 넣지 않는다.
- 200줄로 쓴 코드가 50줄이면 충분한지 다시 검토한다.

### 3. 외과적 변경

- 요청과 직접 관련된 줄만 바꾼다.
- 주변 코드, 주석, 포맷을 임의로 개선하지 않는다.
- 기존 스타일을 따른다.
- 관련 없는 죽은 코드나 문제를 발견하면 언급만 하고 삭제하지 않는다.
- 자신의 변경으로 생긴 미사용 import, 변수, 함수는 정리한다.

모든 변경된 줄은 사용자의 요청과 연결되어야 한다.

### 4. 목표 기반 실행

명령형 요청은 검증 가능한 목표로 바꾼다.

- "검증 추가" -> "잘못된 입력 테스트를 작성하고 통과시킨다."
- "버그 수정" -> "버그를 재현하는 테스트를 만든 뒤 통과시킨다."
- "리팩터링" -> "변경 전후 테스트가 모두 통과하도록 유지한다."

여러 단계 작업은 간단한 계획과 각 단계의 검증 방법을 먼저 제시한다.

## Hugging Face 작업 규칙

Hugging Face 모델을 사용하는 프로젝트는 다음 정보를 기록한다.

- 모델 ID
- 태스크 유형
- 입력 형식
- 출력 형식
- 모델 라이선스
- 필요한 Python 버전과 주요 패키지
- GPU/CPU 요구사항
- 샘플 실행 명령
- 최소 테스트 또는 스모크 테스트 방법

대형 파일과 민감 정보는 조심해서 다룬다.

- API 토큰, 개인 키, `.env` 내용은 커밋하지 않는다.
- 모델 가중치, 캐시, 대형 데이터셋은 기본적으로 저장소에 넣지 않는다.
- 필요한 경우 `.gitignore`에 캐시, 출력물, 임시 파일을 추가하되 먼저 승인받는다.
- 외부 모델 다운로드 전에는 용량, 위치, 목적을 사용자에게 보고한다.

## 공개 저장소 일반화 규칙

이 루트 폴더의 일부 프로젝트는 나중에 GitHub 등 공개 저장소에 올라갈 수 있다. 공개 가능성이 있는 문서, 코드, 설정 예시는 개인 환경에 묶인 정보를 그대로 포함하지 않는다.

다음 정보는 공개 전 일반화하거나 제거한다.

- 개인 사용자명이 들어간 로컬 경로
- `C:\Users\...` 같은 절대 경로
- 개인 계정명, 이메일, 컴퓨터 이름
- API 키, 토큰, 개인 키, 인증 정보
- `.env` 파일 내용
- 로컬 캐시 경로, 모델 다운로드 경로, 데이터셋 원본 경로
- 비공개 프로젝트명이나 내부 서비스 주소

경로를 문서에 써야 할 때는 다음처럼 일반화한다.

```text
C:\Users\사용자명\Desktop\AI_Agent_Lab\venv -> <LOCAL_VENV_PATH>
C:\Users\사용자명\Desktop\AI\my-project -> <PROJECT_ROOT>
C:\Users\사용자명\.cache\huggingface -> <HF_CACHE_DIR>
```

공개 전에는 최소한 다음을 확인한다.

- `C:\Users`, 사용자명, 실제 토큰 문자열이 남아 있지 않은가
- `.env`, 캐시, 모델 가중치, 대형 데이터셋이 포함되지 않았는가
- README, HARNESS, 예시 코드가 개인 PC 경로 대신 플레이스홀더를 쓰는가
- 실행 예시는 다른 사용자가 자신의 환경에 맞게 바꿀 수 있는 형태인가

## 로컬 환경

현재 공용 가상환경 후보는 다음 위치로 일반화해서 문서에 기록한다.

```text
<LOCAL_VENV_PATH>
```

새 프로젝트에서 이 가상환경을 사용할지, 프로젝트별 가상환경을 새로 만들지는 작업 전에 보고하고 승인받는다. 실제 로컬 경로는 공개 가능한 문서에 직접 적지 않는다.

## 프로젝트 작성 기본 형태

새 하위 프로젝트를 만들 때는 승인 후 가능한 한 다음 구조를 우선 검토한다.

```text
project-name/
  README.md
  requirements.txt
  src/
  scripts/
  tests/
  examples/
```

단, 프로젝트가 작으면 더 단순한 구조를 사용한다. 구조는 목적을 따라야 하며, 빈 폴더를 관성적으로 만들지 않는다.

## 완료 보고

작업을 마칠 때는 다음을 간결하게 보고한다.

- 변경한 파일
- 핵심 변경 내용
- 실행한 검증
- 남은 위험이나 확인하지 못한 부분

검증하지 못했다면 이유를 명확히 말한다.
